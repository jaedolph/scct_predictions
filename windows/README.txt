# SCCT Predictions Setup Guide

## Installation

Run the install.bat script.

## Running the program

Run the run.bat script. On first startup, the initial setup utility will run.

## Run on startup

1. Right click the "run.bat" file and select "Show more options" then click "Create shortcut"
2. Press "Windows Key + R"
3. Type "shell:startup" and press enter
4. Move the "run.bat" shortcut file to the startup folder that opens

## Modify configuration

Open http://localhost:5123/configure in your browser or edit the scct_predictions.ini file manually.